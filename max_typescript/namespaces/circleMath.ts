namespace MyMath {
    export namespace Circle {
        const PI = 3.1415926;

        export function calcCircumference(radius: number)
        {
            return radius * 2 * PI;
        }
    }
}